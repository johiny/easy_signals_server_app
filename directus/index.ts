const LoadDirectus = async (callback) => {
    // const { startServer } = await import ('@directus/api/server');
    // await startServer();
    console.log('LoadDirectus')
};

export default LoadDirectus;